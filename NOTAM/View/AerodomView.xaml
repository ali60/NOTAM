<UserControl x:Class="NOTAM.View.AerodomView"      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"    >    <Grid Margin="4">        <Grid.ColumnDefinitions>            <ColumnDefinition Width="Auto" />            <ColumnDefinition Width="6" />            <ColumnDefinition Width="*" />        </Grid.ColumnDefinitions>        <Grid.RowDefinitions>            <RowDefinition Height="Auto" />            <RowDefinition Height="30" />            <RowDefinition Height="Auto" />            <RowDefinition Height="30" />            <RowDefinition Height="Auto" />            <RowDefinition Height="30" />            <RowDefinition Height="Auto" />            <RowDefinition Height="30" />            <RowDefinition Height="Auto" />            <RowDefinition Height="30" />            <RowDefinition Height="Auto" />            <RowDefinition Height="30" />            <RowDefinition Height="Auto" />            <RowDefinition Height="30" />            <RowDefinition Height="Auto" />            <RowDefinition Height="30" />            <RowDefinition Height="Auto" />        </Grid.RowDefinitions>        <Grid.Resources>            <DataTemplate DataType="{x:Type ValidationError}">                <TextBlock           FontStyle="Italic"          Foreground="Red"          HorizontalAlignment="Right"          Margin="0,1"          Text="{Binding Path=ErrorContent}"           />            </DataTemplate>        </Grid.Resources>        <!-- CUSTOMER TYPE -->        <Label       Grid.Row="0" Grid.Column="0"       Content="Code:"       HorizontalAlignment="Right"      Target="{Binding ElementName=codeTxt}"       />        <TextBox       x:Name="codeTxt"      Grid.Row="0" Grid.Column="2"       Text="{Binding Path=Code, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"       Validation.ErrorTemplate="{x:Null}"      />        <ContentPresenter       Grid.Row="1" Grid.Column="2"      Content="{Binding ElementName=customerTypeCmb, Path=(Validation.Errors).CurrentItem}"      />        <!-- NAME-->        <Label       Grid.Row="2" Grid.Column="0"       Content="Name:"       HorizontalAlignment="Right"      Target="{Binding ElementName=nameTxt}"      />        <TextBox       x:Name="nameTxt"      Grid.Row="2" Grid.Column="2"       Text="{Binding Path=Name, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"       Validation.ErrorTemplate="{x:Null}"      />        <ContentPresenter       Grid.Row="3" Grid.Column="2"      Content="{Binding ElementName=nameTxt, Path=(Validation.Errors).CurrentItem}"      />        <!-- FIR -->        <Label       Grid.Row="4" Grid.Column="0"       Content="FIR : "       HorizontalAlignment="Right"      Target="{Binding ElementName=firCmb}"       />        <ComboBox       x:Name="firCmb"      Grid.Row="4" Grid.Column="2"       ItemsSource="{Binding Path=FIROptions, Mode=OneTime}"      SelectedItem="{Binding Path=FIR, ValidatesOnDataErrors=True}"      Validation.ErrorTemplate="{x:Null}" IsEditable="True" />        <ContentPresenter       Grid.Row="5" Grid.Column="2"      Content="{Binding ElementName=firCmb, Path=(Validation.Errors).CurrentItem}"      />        <!--Country-->        <Label       Grid.Row="6" Grid.Column="0"       Content="Country:"       HorizontalAlignment="Right"      Target="{Binding ElementName=countryCmb}"       />        <ComboBox       x:Name="countryCmb"      Grid.Row="6" Grid.Column="2"       ItemsSource="{Binding Path=CountryOptions, Mode=OneTime}"      SelectedItem="{Binding Path=Country, ValidatesOnDataErrors=True}"      Validation.ErrorTemplate="{x:Null}" IsEditable="True" />        <ContentPresenter       Grid.Row="7" Grid.Column="2"      Content="{Binding ElementName=countryCmb, Path=(Validation.Errors).CurrentItem}"      />              <!-- Latitude-->        <Label       Grid.Row="8" Grid.Column="0"       Content="_Latitude:"       HorizontalAlignment="Right"      Target="{Binding ElementName=latitudeTxt}"       />        <TextBox       x:Name="latitudeTxt"      Grid.Row="8" Grid.Column="2"       Text="{Binding Path=Latitude, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"      Validation.ErrorTemplate="{x:Null}"      />        <ContentPresenter       Grid.Row="9" Grid.Column="2"      Content="{Binding ElementName=latitudeTxt, Path=(Validation.Errors).CurrentItem}"      />        <!--Direction-->        <Label       Grid.Row="10" Grid.Column="0"       Content="_Direction:"       HorizontalAlignment="Right"      Target="{Binding ElementName=directionTxt}"       />        <TextBox       x:Name="directionTxt"      Grid.Row="10" Grid.Column="2"       Text="{Binding Path=Direction, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"      Validation.ErrorTemplate="{x:Null}"      />        <ContentPresenter       Grid.Row="11" Grid.Column="2"      Content="{Binding ElementName=directionTxt, Path=(Validation.Errors).CurrentItem}"      />        <!-- VOR-->        <Label       Grid.Row="12" Grid.Column="0"       Content="_VOR:"       HorizontalAlignment="Right"      Target="{Binding ElementName=vorTxt}"       />        <TextBox       x:Name="vorTxt"      Grid.Row="12" Grid.Column="2"       Text="{Binding Path=VOR, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"      Validation.ErrorTemplate="{x:Null}"      />        <ContentPresenter       Grid.Row="13" Grid.Column="2"      Content="{Binding ElementName=vorTxt, Path=(Validation.Errors).CurrentItem}"      />        <!-- IL-->        <Label       Grid.Row="14" Grid.Column="0"       Content="_I_L:"       HorizontalAlignment="Right"      Target="{Binding ElementName=ilTxt}"       />        <TextBox       x:Name="ilTxt"      Grid.Row="14" Grid.Column="2"       Text="{Binding Path=IL, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"      Validation.ErrorTemplate="{x:Null}"      />        <ContentPresenter       Grid.Row="15" Grid.Column="2"      Content="{Binding ElementName=ilTxt, Path=(Validation.Errors).CurrentItem}"      />        <!-- SAVE BUTTON -->        <Button       Grid.Row="16" Grid.Column="2"      Command="{Binding Path=SaveCommand}"      Content="_Save"      HorizontalAlignment="Right"      Margin="4,2"       MinWidth="60"       />    </Grid></UserControl>